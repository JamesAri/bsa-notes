# ssh
cd etc/ssh/ssh_config
cd etc/ssh/sshd_config

# sudo settings
cd /etc/sudoers.d

# ldap
vim /etc/nsswitch.conf

# configuration file used by the libnss-ldap, allows the system to use
# LDAP (Lightweight Directory Access Protocol) as a backend for system
# databases like passwd, group, and shadow
vim /etc/libnss-ldap.conf

# ldap
vim /etc/ldap/ldap.conf
vim /etc/pam_ldap.conf

# pam config
cd /etc/pam.d


# (Encrypted Partitions)
vim /etc/crypttab
vim /etc/fstab
